% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/choose_params_values.R
\name{choose_params_values}
\alias{choose_params_values}
\title{Render a table to give an indication of the values to choose for the prevalence level and the top filtering percentage}
\usage{
choose_params_values(
  data_with_annotation,
  object_of_interest,
  list_sims,
  prev_list = NULL,
  filtering_list = c(20),
  graph_file = NULL,
  col_module_id,
  annotation_level,
  seed = NULL
)
}
\arguments{
\item{data_with_annotation}{Dataframe. The abundance table merged with the module names. Required format: modules are the rows and samples are the columns. The first column must be the modules name (e.g. species), the second is the module ID (e.g. msp), and each subsequent column is a sample}

\item{object_of_interest}{String. The name of the bacteria or species of interest or a key word in the functional module definition}

\item{list_sims}{List of dataframes. The result of simulate_by_prevalence()}

\item{prev_list}{List of numeric. The prevalences to be studied. Required format is decimal: 0.20 for 20\% of prevalence}

\item{filtering_list}{List of numeric. The filtering top percentages to be studied. Required format is: 10 for the top 10\%}

\item{graph_file}{Dataframe. The object generated by graph_step() function}

\item{col_module_id}{String. The name of the column with the module names in annotation_table}

\item{annotation_level}{String. The name of the column with the level to be studied. Examples: species, genus, level_1}

\item{seed}{Numeric. The seed number, ensuring reproducibility}
}
\value{
Dataframe. Returns F1 rates before and after using NeighborFinder()
}
\description{
Render a table to give an indication of the values to choose for the prevalence level and the top filtering percentage
}
\examples{
data(data)
data(graphs)
simulations <- simulate_by_prevalence(data$CRC_JPN, prev_list=c(0.20,0.30), graph_file=graphs$CRC_JPN, col_module_id="msp_id", annotation_level="species", seed=20232024)
choose_params_values(data$CRC_JPN, "Klebsiella", simulations, filtering_list=c(10,20), graph_file = graphs$CRC_JPN, col_module_id="msp_id", annotation_level="species", seed = 20232024)
}
